<h1 class="glow-text">📜 Einweisungen für <%= ctNumber %></h1>

<div class="container">
    <% if (instructions.length === 0) { %>
        <p>✅ Keine Einweisungen für diesen Benutzer gefunden.</p>
    <% } else { %>
        <ul class="stat-list">
            <% instructions.forEach(instruction => { %>
                <li>
                    👤 <b>Eingewiesene Person:</b> <%= instruction.trainee_username %> (<@<%= instruction.trainee_user_id %>>) <br>
                    🎮 <b>VRC-Name:</b> <%= instruction.vrc_name %> <br>
                    🏅 <b>Ausbilder:</b> <%= instruction.instructor_username %> (CT-<%= instruction.ct_number %>) <br>
                    📅 <b>Datum:</b> <%= new Date(instruction.timestamp).toLocaleDateString("de-DE") %> <br>
                </li>
            <% }); %>
        </ul>
    <% } %>
</div>
